import * as React from "react";

import logo from "../../macke-logo.png";

export const Logo = (props) => (
  <div style={{ marginTop: "1em" }}>
    <img src={logo} alt="React-Macke" width="300px" />
  </div>
);
